[package]
name = "qtv-vm-fuzz"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Fuzz harness that feeds random bytes and random programs to the decoder and interpreter."

[dependencies]
qtv-vm = { path = "../crates/qtv-vm" }
