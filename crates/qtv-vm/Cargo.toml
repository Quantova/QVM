[package]
name = "qtv-vm"
version = "0.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Quantova register virtual machine core with instruction set, deterministic interpreter, gas model, bytecode container, and assembler."

[dependencies]
qtv-crypto = { git = "https://github.com/Quantova/Q-Crypto.git", tag = "v0.1.0" }
